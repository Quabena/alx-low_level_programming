#include "main.h"

/**
* add - Adds two numbers / integers
* @a: First number to add
*
* @b: Second Number to add
*
* Return: The sum of the two integers
*/

int add(int a, int b)
{
  return (a + b);
}
